# {CNG} Accounts micro service
Golang with Gin router and Postgres as database.

### Running service:

#### `$ make` 

### Avaliable routes:

#### POST
- /api/validate
- /api/new-account
- /api/account-details
